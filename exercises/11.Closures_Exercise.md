# 📝 Exercise 11: Closures (`11.closures.js`)

### Questions:
1. What is **Lexical Scope** in JavaScript?
2. How does a function "remember" variables even after its parent function has finished?
3. What is the main benefit of using a closure for variable storage?
4. In the "Counter" example, why do multiple counters (counter1, counter2) not share the same value?

### Practice Task:
- Create a file named `solution11.js`.
- Write a function `createGame` that has a private variable `score` initialized to 0.
- Return an object with two methods: `win()` (adds 1 to score) and `getScore()` (returns the score).
- Try to access the `score` variable directly from outside and observe the result.
- Write a function `multiplier(factor)` that returns an anonymous function. The returned function should take a `number` and return `number * factor`.
- Create a `triple` function using `multiplier(3)` and test it.
